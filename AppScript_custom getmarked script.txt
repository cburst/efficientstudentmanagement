function duplicateForm() {
  var templateformId = '11wnuELfvLEMFMWE7EKZsjieMdlg4TteljfM-M7ZiuyY';
  var destFolder = DriveApp.getFolderById("1-jOTWTHLYUy3H23Q4mhDmqGKtO_70I9V");
  
  // Define the variable with the desired title
  var formTitle = "Everyday English TEMPLATE";
  
  // Copy the template form to the destination folder with the same title
  var file = DriveApp.getFileById(templateformId).makeCopy(formTitle, destFolder);
  
  // Get the copied form's ID
  var fileId = file.getId();
  
  // Open the copied form using FormApp
  var form = FormApp.openById(fileId);
  
  // Set both the file name and form title to the same value
  form.setTitle(formTitle);
  
  // Set the custom form description
  var customDescription = "TEMPLATE TEXT HERE";
  form.setDescription(customDescription);
  
  // Set other form properties and settings as needed
var item = form.addMultipleChoiceItem();item.setTitle('It that the senator is to retire at the end of her term.').setChoices([item.createChoice(' has announced', false), item.createChoice(' announced it', false), item.createChoice(' has been announced', true)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('Excuse me. __ to be a problem with this seat-I can\'t change its position.').setChoices([item.createChoice(' It seems', false), item.createChoice(' There seems', true), item.createChoice(' It appears', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('__ to a recent article, eating a lot of salt may not cause long-term health problems.').setChoices([item.createChoice(' According', true), item.createChoice(' Apparently', false), item.createChoice(' Considering', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('__ that the murderer is being concealed by friends.').setChoices([item.createChoice(' There is thought', false), item.createChoice(' It is thought', true), item.createChoice(' It thought', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('My house is a mess-if only I __ so messy!').setChoices([item.createChoice(' \'m not', false), item.createChoice(' weren\'t', true), item.createChoice(' was', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('I\'d __ you didn\'t come in with your muddy shoes.').setChoices([item.createChoice(' rather', true), item.createChoice(' prefer it', false), item.createChoice(' wish', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('I really wish we __ that white sofa-it gets dirty much too easily.').setChoices([item.createChoice(' haven\'t bought', false), item.createChoice(' hadn\'t bought', true), item.createChoice(' don\'t buy', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('I\'d like __ it in the morning, if that\'s possible.').setChoices([item.createChoice(' that they deliver', false), item.createChoice(' them delivering', false), item.createChoice(' them to deliver', true)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('If we hadn\'t had to work late, __ the game now.').setChoices([item.createChoice(' I\'d be watching', true), item.createChoice(' I\'d have watched', false), item.createChoice(' I\'ll watch', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('I\'ll pay for the classes __ you agree not to miss any.').setChoices([item.createChoice(' supposing', false), item.createChoice(' unless', false), item.createChoice(' providing', true)]);item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('The president is believed _______ his vacation in the Caribbean currently. (spend)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('It\'s time you _______ to think about what subjects you want to study next year. (start)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('My parents always encouraged me ______ foreign languages. (learn)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('My new job involves me _______ to South America two or three times a year. (travel)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('They\'re incredibly generous people and they wouldn\'t let me ______ for anything. (pay)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('Daniel can stay the night as long as he _____ sleeping on the sofa. (not mind)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('Supposing the Yankees lost their last two games, ___ they still ___ the division? (win)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('Marcus might have hurt his head badly if he ___ a helmet when he fell off his bike. (not wear)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('___ you ___ me earlier that you were coming, I would have taken the day off. (tell)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('If my wife hadn\'t inherited a lot of money, we definitely _______ in a house like this now. (not live)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('Do you think I should _______ to Mario yesterday? (apologize)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('You\'d better _______ to the doctor about that cough. (go)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('You\'re not supposed _______ your cell phone at work, but everyone does. (use)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('Alex seems _______ a lot recently. Do you think he\'s studying enough? (go out)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('Isn\'t there anywhere _______ here? (sit down)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('Rick hates _______ that he doesn\'t dance very well. (admit)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('I would love _______ the exhibition, but it finished the day before we arrived. (see)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('There\'s no point _______ him. He always has his phone turned off while he\'s driving. (call)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('It\'s important for celebrities _______ at all the right parties. (see)');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('Let\'s go and have a coffee. The meeting isn\'t due ______ until 10:30. (start)');item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('It is _________- to wear jewelry at school.').setChoices([item.createChoice(' not allowed', true), item.createChoice(' not permitted', false), item.createChoice(' (both)', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('______________________ to my advice, but it\'s too late now.').setChoices([item.createChoice(' You should have listened', true), item.createChoice(' You should listen', false), item.createChoice(' (both)', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('I\'ll have ____________-, so you\'ll easily recognize me at the airport.').setChoices([item.createChoice(' white suit', false), item.createChoice(' be wearing', false), item.createChoice(' (both)', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('_____________ down today. Is everything OK?').setChoices([item.createChoice(' You look', false), item.createChoice(' You seem', false), item.createChoice(' (both)', true)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('It smells __________- someone has burned the toast.').setChoices([item.createChoice(' as if', false), item.createChoice(' as though', false), item.createChoice(' (both)', true)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('Is that your father upstairs? _____________- his voice.').setChoices([item.createChoice(' I can hear', true), item.createChoice(' I\'m hearing', false), item.createChoice(' (both)', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('This coffee tea. It\'s undrinkable!').setChoices([item.createChoice(' tastes like', true), item.createChoice(' tastes of', false), item.createChoice(' (both)', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('________________ at home this afternoon, so you can call me there.').setChoices([item.createChoice(' I\'ll be working', false), item.createChoice(' I\'m working', false), item.createChoice(' (both)', true)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('You\'d better get on the train now. It ____________ leave.').setChoices([item.createChoice(' is to', false), item.createChoice(' is about to', true), item.createChoice(' (both)', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('The mayor ___________ open the new hospital early next month.').setChoices([item.createChoice(' is to', false), item.createChoice(' is due to', false), item.createChoice(' (both)', true)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('A: Can I come with you? B: I suppose _______, but hurry up.').setChoices([item.createChoice(' so', true), item.createChoice(' yes', false), item.createChoice(' (both)', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('A: You have to read her latest book. B: I already ______.').setChoices([item.createChoice(' am', false), item.createChoice(' have', false), item.createChoice(' (both)', true)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('Look, I found an old ____________ in the attic!').setChoices([item.createChoice(' photo album', false), item.createChoice(' album of photos', false), item.createChoice(' (both)', true)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('Come over about 9:00. We\'ll be ___________.').setChoices([item.createChoice(' at Alex\'s', false), item.createChoice(' at Alex\'s house', false), item.createChoice(' (both)', true)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('I can\'t find my ___________. Have you seen them?').setChoices([item.createChoice(' car\'s keys', false), item.createChoice(' car keys', true), item.createChoice(' (both)', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('She has two sisters, __________.').setChoices([item.createChoice(' both of whom are very pretty', false), item.createChoice(' who are both very pretty', false), item.createChoice(' (both)', true)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('I got exactly ________ I wanted for my birthday-a Kindle.').setChoices([item.createChoice(' that', false), item.createChoice(' what', true), item.createChoice(' (both)', false)]);item.setPoints(1);item.setRequired(true);var item = form.addMultipleChoiceItem();item.setTitle('Her aunt, _______ never usually said a word, suddenly burst out laughing.').setChoices([item.createChoice(' who', true), item.createChoice(' that', false), item.createChoice(' (both)', false)]);item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('I didn\'t bring sunscreen because the weather forecast said rain. The _______ because the weather forecast said rain. (rewrite with \'reason\')');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('I spoke to the head of customer service. The _______ the head of customer service. (rewrite with \'person\')');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('I don\'t like the way he blames other people for his mistakes. _______ the way he blames other people for his mistakes. (rewrite with \'what\')');item.setPoints(1);item.setRequired(true);var item = form.addParagraphTextItem();item.setTitle('I only said that I thought she was making a big mistake marrying him. _______ I thought that she was making a big mistake marrying him. (rewrite with \'all\')');item.setPoints(1);item.setRequired(true);var item = form.addTextItem();item.setTitle('A: boy from my school was chosen to carry the Olympic torch. _______ was chosen to carry the Olympic torch. (rewrite with \'it\')');item.setPoints(1);item.setRequired(true);

  // Get the published URL of the form
  var formUrl = form.getPublishedUrl();

  // Shorten the response URL using the built-in function (if available).
var shortenedUrl = form.shortenFormUrl(formUrl);

// Get the URL for editing the form
  var editUrl = form.getEditUrl();
  
  // Log the form URL to the Google Apps Script log
  Logger.log('Shortened Form URL: ' + shortenedUrl);
  Logger.log('Edit URL: ' + editUrl);
}function duplicateForm() {
  var templateformId = '[INSERT GDRIVE TEMPLATE FORM FILE IDENTIFIER HERE]';
  var destFolder = DriveApp.getFolderById("[INSERT GDRIVE FOLDER IDENTIFIER HERE]");
  
  // Define the variable with the desired title
  var formTitle = "[INSERT FORM TITLE HERE]";
  
  // Copy the template form to the destination folder with the same title
  var file = DriveApp.getFileById(templateformId).makeCopy(formTitle, destFolder);
  
  // Get the copied form's ID
  var fileId = file.getId();
  
  // Open the copied form using FormApp
  var form = FormApp.openById(fileId);
  
  // Set both the file name and form title to the same value
  form.setTitle(formTitle);
  
  // Set the custom form description
  var customDescription = "[INSERT FORM DESCRIPTION HERE]";
  form.setDescription(customDescription);
  
  // Set other form properties and settings as needed
[INSERT GETMARKED SCRIPT CONTENT HERE STARTING WITH var item]

  // Get the published URL of the form
  var formUrl = form.getPublishedUrl();

  // Shorten the response URL using the built-in function (if available).
var shortenedUrl = form.shortenFormUrl(formUrl);

// Get the URL for editing the form
  var editUrl = form.getEditUrl();
  
  // Log the form URL to the Google Apps Script log
  Logger.log('Shortened Form URL: ' + shortenedUrl);
  Logger.log('Edit URL: ' + editUrl);
}